package main

import (
	"fmt"
	"rsc.io/quote/v4"
)

func main() {
	fmt.Println(quote.Go())
}


// Terminal Commands
go mod init // creates a place to store modules
go run . // runs main (I think?)
go mod tidy // downloads all modules that need to be imported
